# servidorkivymd
Um código que utiliza kivymd e cliente/servidor Modbus

Para utilizar, deve se certificar que o servidor está aberto em um terminal e em outro rodar o main para utilizar a interface gráfica em kivymd
